{
  "info": {
    "name": "Liana Plant API",
    "description": "API collection for Liana Plant application",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "User",
      "item": [
        {
          "name": "Get User",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/user",
              "host": ["{{base_url}}"],
              "path": ["user"]
            },
            "description": "Get authenticated user information"
          }
        }
      ]
    },
    {
      "name": "Test",
      "item": [
        {
          "name": "Test Endpoint",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/test",
              "host": ["{{base_url}}"],
              "path": ["test"]
            },
            "description": "Test endpoint that returns true"
          }
        }
      ]
    },
    {
      "name": "FCM Tokens",
      "item": [
        {
          "name": "Get Saved Tokens",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/get-saved-tokens",
              "host": ["{{base_url}}"],
              "path": ["get-saved-tokens"]
            },
            "description": "Get FCM tokens for masters"
          }
        }
      ]
    },
    {
      "name": "Masters",
      "item": [
        {
          "name": "Get All Masters",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/masters",
              "host": ["{{base_url}}"],
              "path": ["masters"]
            },
            "description": "Get list of all masters"
          }
        },
        {
          "name": "Add Review",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{base_url}}/masters/review",
              "host": ["{{base_url}}"],
              "path": ["masters", "review"]
            },
            "description": "Add a review for a master (requires authentication)"
          }
        },
        {
          "name": "Get Master",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/masters/:id",
              "host": ["{{base_url}}"],
              "path": ["masters", ":id"]
            },
            "description": "Get specific master details"
          }
        },
        {
          "name": "Update Work Schedule",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{base_url}}/masters/:id/work-schedule",
              "host": ["{{base_url}}"],
              "path": ["masters", ":id", "work-schedule"]
            },
            "description": "Update master's work schedule"
          }
        },
        {
          "name": "Set Available",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{base_url}}/masters/:id/availability",
              "host": ["{{base_url}}"],
              "path": ["masters", ":id", "availability"]
            },
            "description": "Set master as available"
          }
        }
      ]
    },
    {
      "name": "Appointments",
      "item": [
        {
          "name": "Check if Busy",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/appointments/is-busy",
              "host": ["{{base_url}}"],
              "path": ["appointments", "is-busy"]
            },
            "description": "Check if a time slot is busy"
          }
        },
        {
          "name": "Get Booked Slots",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/appointments/booked-slots",
              "host": ["{{base_url}}"],
              "path": ["appointments", "booked-slots"]
            },
            "description": "Get all booked time slots"
          }
        },
        {
          "name": "Book Appointment",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{base_url}}/appointments/book",
              "host": ["{{base_url}}"],
              "path": ["appointments", "book"]
            },
            "description": "Book a new appointment"
          }
        }
      ]
    },
    {
      "name": "Services",
      "item": [
        {
          "name": "Get All Services",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/services",
              "host": ["{{base_url}}"],
              "path": ["services"]
            },
            "description": "Get list of all services"
          }
        },
        {
          "name": "Get Service",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/services/:id",
              "host": ["{{base_url}}"],
              "path": ["services", ":id"]
            },
            "description": "Get specific service details"
          }
        },
        {
          "name": "Get Services for Master",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/services/get-for-master/:master_id",
              "host": ["{{base_url}}"],
              "path": ["services", "get-for-master", ":master_id"]
            },
            "description": "Get services available for a specific master"
          }
        }
      ]
    },
    {
      "name": "Authentication",
      "item": [
        {
          "name": "Master Register",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{base_url}}/auth/master-register",
              "host": ["{{base_url}}"],
              "path": ["auth", "master-register"]
            },
            "description": "Register a new master"
          }
        },
        {
          "name": "Client Register",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{base_url}}/auth/client-register",
              "host": ["{{base_url}}"],
              "path": ["auth", "client-register"]
            },
            "description": "Register a new client"
          }
        },
        {
          "name": "Send Verification Code",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{base_url}}/auth/send-code",
              "host": ["{{base_url}}"],
              "path": ["auth", "send-code"]
            },
            "description": "Send SMS verification code"
          }
        },
        {
          "name": "Verify Code",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{base_url}}/auth/verify-code",
              "host": ["{{base_url}}"],
              "path": ["auth", "verify-code"]
            },
            "description": "Verify SMS code"
          }
        }
      ]
    },
    {
      "name": "Protected Routes",
      "item": [
        {
          "name": "Protected Method",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/protected-route",
              "host": ["{{base_url}}"],
              "path": ["protected-route"]
            },
            "description": "Access protected route (requires authentication)"
          }
        }
      ]
    }
  ],
  "variable": [
    {
      "key": "base_url",
      "value": "http://localhost:8000/api/v1",
      "type": "string"
    }
  ]
} 